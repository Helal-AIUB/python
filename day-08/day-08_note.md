
## 1. What is OOP?
OOP (Object-Oriented Programming) is a programming paradigm that organizes code using classes and objects. It makes programs reusable, organized, and easier to maintain.

## 2. What is a class?
A class is a blueprint or template for creating objects. It defines the attributes (variables) and methods (functions) of an object.

## 3. What is an object?
An object is an instance of a class. It contains real data and can use the methods defined in the class.

## 4. What is __init__?
`__init__` is a special constructor method that runs automatically when an object is created. It is used to initialize object attributes.

## 5. What is self?
`self` refers to the current object of the class. It is used to access the object's attributes and methods.

## 6. What is inheritance?
Inheritance is an OOP feature where one class inherits the properties and methods of another class. It helps reduce code duplication and improves code reusability.

## 7. Why is OOP important for Django?
Django is built using OOP principles. Models, views, forms, and many other components are Python classes, making the framework modular and reusable.

## 8. How is a Django model related to a Python class?
A Django model is a Python class that inherits from `models.Model`. Each model represents a database table, and each object represents a row in that table.

## 9. Explain your final project structure.
My final project is organized into separate files and modules. Different functions are used for different tasks, making the project clean, readable, and easy to maintain.

## 10. What problems did you face during the final project?
I faced problems with debugging, organizing the code, handling file operations, and fixing logical errors. I solved them through practice and testing.

## 11. What did you learn today?
Today I learned the basics of Object-Oriented Programming, including classes, objects, constructors (`__init__`), `self`, inheritance, and why OOP is important in Django.